import { WagonColorDirective } from './wagon-color.directive';

describe('WagonColorDirective', () => {
  it('should create an instance', () => {
    const directive = new WagonColorDirective();
    expect(directive).toBeTruthy();
  });
});
